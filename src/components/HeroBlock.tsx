import React from 'react';

const HeroBlock = () => {
  return (
    <div style={{ background: '#dbeafe', padding: '2rem', marginBottom: '1rem' }}>
      <h2>🚀 Hero Block</h2>
      <p>This is a hero block with a heading, subtitle, and CTA.</p>
    </div>
  );
};

export default HeroBlock;
